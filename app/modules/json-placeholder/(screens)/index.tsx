import { SafeAreaView, Text } from "react-native";

export default function Index() {
  return (
    <SafeAreaView>
      <Text>
        JSON Placeholder is a free online service that functions as a mock REST
        API. It provides developers with readily available fake data in JSON
        format, which is useful for various purposes, particularly during the
        initial development stages.
      </Text>
    </SafeAreaView>
  );
}
